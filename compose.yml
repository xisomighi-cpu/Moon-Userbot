services:
    dexbot:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: dexbot
        restart: unless-stopped
        env_file:
            - .env
        networks:
            - default

networks:
    default:
        external: false